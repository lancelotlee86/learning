# Chapter 2

几种相似度算法的选择
1. 如果数据稠密（几乎所有属性都没有零值），且属性值大小非常重要，那么使用诸如**欧式距离**或者**曼哈顿距离**
2. 如果数据手分数贬值（grade-inflation，即不用用户使用不用的评级范围）的影响，则使用**皮尔逊相关系数**
3. 如果数据稀疏，考虑使用**余弦相似度**

依赖于单个“最相似”的用户进行推荐会使该用户的任何“怪癖”都被推荐，可以通过基于多个相似的用户进行推荐，这里可以使用**k近邻方法**。
主要思路就是，找到k个最相似的邻居，以他们的皮尔逊系数占比，来决定每个人的影响程度。